# Only needed when `auth` is set to "key" in config.json.
# The default auth is "oauth" — the librarian shells out to your existing
# `claude` CLI login, so NO API key is required out of the box.
#
# Copy to .env (gitignored) and fill in only if you opt into key auth:
ANTHROPIC_API_KEY=
